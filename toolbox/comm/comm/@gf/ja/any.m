% ANY   GFベクトルの任意の要素の非ゼロを検出
%
% GFベクトルに対して、ベクトルの要素のいずれかが非ゼロの場合、ANY(V) は、
% 1を出力します。そうでなければ、0を出力します。行列に対して、ANY(X) は、
% X の列に対して演算を行い、1と0を要素とする行ベクトルを出力します。
%
% ANY(X,DIM) は、次元 DIM に対して演算を行います。たとえば、ANY(X,1) は、
% X の最初の次元(行)に対して演算を行います。
%
% 参考 : ALL.


%    Copyright 1996-2002 The MathWorks, Inc.
