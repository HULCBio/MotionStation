<(area)LEVEL must be a scalar.
>(area)LEVELはスカラである必要があります
<(clabel)   Carefully select contours for labeling.
>(clabel)   ラベリングを行うコンターを選択してください
<(clabel)   When done, press RETURN while the Graph window is the active window.
>(clabel)   終わった場合は、グラフィックスウィンドウをアクティブにしてリターンキーを押してください
<(clabel)First input must be a valid contour description matrix.
>(clabel)最初の入力は適切なコンター表現行列である必要があります
<(clabel)Not enough contour handles.
>(clabel)コンターハンドルが十分でありません
<(colstyle)Requires one string argument.
>(colstyle)1つの文字列引数が必要です
<(compass)X and Y must be same length.
>(compass)XとYは同じ長さである必要があります
<(coneplot)Cx,Cy,Cz must all be the same size.
>(coneplot)Cx,Cy,Czは同じサイズである必要があります
<(coneplot)S must be a scalar.
>(coneplot)Sはスカラである必要があります
<(errorbar)Arguments must be numeric.
>(errorbar)引数は数値である必要があります
<(errorbar)The sizes of X, Y, L and U must be the same.
>(errorbar)X、Y、LとUのサイズは同じである必要があります
<(ezgraph3)Cannot plot in 4 dimensions
>(ezgraph3)4次元はプロットできません
<(ezgraph3)Incorrect inputs.
>(ezgraph3)入力が正しくありません
<(ezgraph3)Mathematical expression required.
>(ezgraph3)数学表現が必要です
<(ezgraph3)To plot parametric curves, use EZPLOT3.
>(ezgraph3)パラメトリックカーブをプロットするにはEZPLOT3を利用してください
<(ezplot)Cannot plot parametrized surfaces.  Try ezsurf.
>(ezplot)パラメトリックなサーフェースはプロットできません。  ezsurfを試してみてください
<(ezplot3)To plot parametrized surfaces, use EZSURF
>(ezplot3)パラメータ化したサーフェースをプロットするにはEZSURFをご利用ください
<(ezpolar)Cannot plot polar curves of more than 1 variable.
>(ezpolar)1変数以上の極座標曲線がプロットできません
<(feather)First 1 or 2 numeric arguments must be numeric.
>(feather)最初の１つ、または2つ目の引数は数値である必要があります
<(feather)First argument must be numeric.
>(feather)最初の引数は数値である必要があります
<(feather)X and Y must be the same length.
>(feather)XとYは同じ長さである必要があります
<(isocaps)Isovalue must be a scalar.
>(isocaps)isovalueはスカラである必要があります
<(isocaps)WHICHPLANE can be: all, xmin, xmax, ymin, ymax, zmin, zmax.
>(isocaps)WHICHPLANEの形式: all、xmin、xmax、ymin、ymax、zmin、zmax
<(isonormals)The handle supplied must be a patch handle
>(isonormals)Patchのハンドルを指定する必要があります
<(makebars)Unrecognized option "%s".
>(makebars)オプション"%s"は正しくありません
<(meshc)The surface Z must contain more than one row or column.
>(meshc)サーフェースZは1つ以上の行、または列をもつ必要があります
<(meshz)Invalid input argument.
>(meshz)入力引数が正しくありません
<(pareto)Y must be a vector.
>(pareto)Yはベクトルである必要があります
<(pcolor)Must have one or three input arguments.
>(pcolor)1、または3つの入力引数が必要です
<(pcolor)Too few input arguments.
>(pcolor)入力引数が少なすぎます
<(pie)X and EXPLODE must be the same length.
>(pie)XとEXPLODEは同じ長さである必要があります
<(pie3)Cell array of strings must be the same length as X.
>(pie3)文字列のセル配列はXと同じ長さである必要があります
<(pie3)Too many arguments or invalid string cell array.
>(pie3)引数が多すぎるか、または正しくない文字列セル配列です
<(plotmatrix)X and Y must be 2-D.
>(plotmatrix)XとYは2-Dである必要があります
<(plotmatrix)X and Y must have the same number of rows and pages.
>(plotmatrix)XとYは行数とページ数が同じである必要があります
<(quiver)Unknown option "%s".
>(quiver)オプション"%s"が正しくありません
<(quiver3)The sizes of X,Y,Z,U,V, and W must all be the same.
>(quiver3)X、Y、Z、U、VとWのサイズはすべて同じである必要があります
<(reducepatch)Faces array contains values which exceed vertices length
>(reducepatch)Faces配列がVerticesの長さを越える値を含んでいます
<(reducepatch)Faces array must contain values >=1 
>(reducepatch)Feces配列は1以上の値である必要があります
<(reducepatch)Reduction must be a scalar.
>(reducepatch)Reductionはスカラである必要があります
<(reducepatch)Reduction must be finite and positive.
>(reducepatch)Reductionは正の有限な値である必要があります
<(reducepatch)The first argument must be a patch handle or a struct containing faces and vertices.
>(reducepatch)最初の引数はPatchハンドル、またはfacesとverticesを含んだ構造体である必要があります
<(reducevolume)Reduction must be [Rx Ry Rz] or R
>(reducevolume)Reductionは[Rx Ry Rz]、またはRである必要があります
<(reducevolume)Reduction too high. Try a smaller reduction.
>(reducevolume)Reductionが高すぎます。 低いReductionで試してみてください
<(reducevolume)Reduction values must be integers greater than or equal to 2
>(reducevolume)Reduction値は2と同じ、またはそれ以上の整数である必要があります
<(rgbplot)Must be a 3-column colormap matrix.
>(rgbplot)3列のカラーマップ行列が必要です
<(rose)Input arguments must be numeric.
>(rose)入力引数は数値である必要があります
<(rotate)H must contain axes children only.
>(rotate)ＨはAxes childrenのみを含む必要があります
<(scatter)S must be a scalar or a vector the same length as X.
>(scatter)Sはスカラ、またはXと同じ長さのベクトルである必要があります
<(scatter)X and Y must be vectors of the same length.
>(scatter)XとYは同じ長さのベクトルである必要があります
<(scatter3)X,Y, and Z must be vectors of the same length.
>(scatter3)X、Y、及びZは同じ長さのベクトルである必要があります
<(shrinkfaces)Shrink factor must be a scalar.
>(shrinkfaces)Shrink factorはスカラである必要があります
<(shrinkfaces)Shrink factor must be positive.
>(shrinkfaces)Shrink factorは正である必要があります
<(slice)V must be a 3-D array.
>(slice)Vは3D配列である必要があります
<(smooth3)All elements of SIZE must be odd integers greater than or equal to 1.
>(smooth3)SIZEのすべての要素は1、またはそれ以上の奇数整数である必要があります
<(smooth3)SIZE must be a scalar or a 3 element vector.
>(smooth3)SIZEはスカラ、または3要素のベクトルである必要があります
<(smooth3)The first parameter must be a scalar or a 1-by-3 size vector.
>(smooth3)最初のパラメータはスカラ、または1 x 3サイズのベクトルである必要があります
<(smooth3)Unknown filter.
>(smooth3)フィルタが正しくありません
<(smooth3)V must be a 3D array.
>(smooth3)Vは3D配列である必要があります
<(stream2)Maximum number of vertices must greater than 0.
>(stream2)verticesの最大値は0より大きい必要があります
<(stream2)STARTX,STARTY must all be the same size.
>(stream2)STARTX、STARTYは同じサイズである必要があります
<(stream2)Step size must greater than 0.
>(stream2)ステップサイズは0より大きい必要があります
<(stream3)STARTX,STARTY,STARTZ must all be the same size.
>(stream3)STARTX、STARTY、STARTZは同じサイズである必要があります
<(streamline)Stream vertices must be passed in as a cell array
>(streamline)Stream verticesはセル配列で指定する必要があります
<(subvolume)In reduction vector, xmax must be greater than or equal to xmin
>(subvolume)reductionベクトルxmaxはxminと同じ、またはそれ以上の値である必要があります
<(subvolume)In reduction vector, ymax must be greater than or equal to ymin
>(subvolume)reductionベクトルymaxはyminと同じ、またはそれ以上の値である必要があります
<(subvolume)In reduction vector, zmax must be greater than or equal to zmin
>(subvolume)reductionベクトルzmaxはzminと同じ、またはそれ以上の値である必要があります
<(subvolume)Reduction must be [xmin xmax ymin ymax zmin zmax]
>(subvolume)Reductionは[xmin xmax ymin ymax zmin zmax]である必要があります
<(surfc)Property value pairs expected.
>(surfc)プロパティ値のペアを入力してください


% $Revision: 1.7 $ $Date: 2002/03/23 02:29:56 $
