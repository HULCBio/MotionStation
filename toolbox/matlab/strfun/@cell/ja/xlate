<(deblank)S must be a cell array.
>(deblank)Sはセル配列である必要があります
<(strcat)All the inputs must be the same size or scalars.
>(strcat)すべての入力引数は同じサイズ、またはスカラである必要があります
<(strcat)Inputs must be cell arrays or strings.
>(strcat)入力はセル配列、または文字列である必要があります
<(strmatch)FLAG must be a string.
>(strmatch)FLAGは文字列である必要があります
<(strmatch)Requires character array or cell array of strings as inputs.
>(strmatch)入力として、文字列配列、または文字列のセル配列が必要です

% $Revision: 1.4 $ $Date: 2002/02/05 13:45:08 $
