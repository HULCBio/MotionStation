<(airy)The first argument must be 0, 1, 2 or 3
>(airy)最初の引数は0、1、2、または3である必要があります
<(betainc)Requires three input arguments.
>(betainc)3つの入力引数が必要です
<(betainc)X must be in the interval [0,1].
>(betainc)Xは[0,1]の範囲である必要があります
<(cross)A and B must be of length 3 in the dimension in which the cross product is taken.
>(cross)外積を実行するには、AとBが3つの次元要素を含んだ行列が必要です
<(cross)A and B must be same size.
>(cross)AとBは同じサイズである必要があります
<(cross)A and B must have at least one dimension of length 3.
>(cross)AとBは少なくとも、長さ3の次元要素をもつ必要があります
<(ellipj)Input arguments must be real.
>(ellipj)引数は実数である必要があります
<(ellipj)M must be in the range 0 <= M <= 1.
>(ellipj)Mは0 <= M <= 1の範囲にある必要があります
<(ellipj)U and M must be the same size.
>(ellipj)UとMは同じサイズである必要があります
<(erf)X must be real.
>(erf)Xは実数である必要があります
<(erfcore)Input argument must be real.
>(erfcore)引数は実数である必要があります
<(erfinv)Y must be real.
>(erfinv)Yは実数である必要があります
<(factor)N must be a positive integer.
>(factor)Nは正の整数である必要があります
<(factor)N must be a scalar.
>(factor)Nはスカラである必要があります
<(factorial)N must be a positive integer
>(factorial)Nは正の整数である必要があります
<(gammaln)Input arguments must be real and nonnegative.
>(gammaln)入力引数は非負の実数である必要があります
<(gcd)Inputs must be the same size.
>(gcd)入力はすべて同じサイズである必要があります
<(gcd)Requires integer input arguments.
>(gcd)整数入力引数が必要です
<(hsv2rgb)H,S,V must all be the same size.
>(hsv2rgb)H,S,Vは同じサイズである必要があります
<(lcm)Input arguments must contain positive integers.
>(lcm)入力引数は正の整数である必要があります
<(legendre)N must be a positive scalar < %d.
>(legendre)Nは %d以下の正のスカラである必要があります
<(nchoosek)K must be an integer between 0 and N
>(nchoosek)Kは0とNの範囲の整数である必要があります
<(nchoosek)Requires 2 input arguments.
>(nchoosek)2つの入力引数が必要です
<(nchoosek)The first argument has to be a scalar or a vector
>(nchoosek)最初の引数はスカラ、またはベクトルである必要があります
<(nchoosek)The second input has to be a non-negative integer
>(nchoosek)2つ目の入力は非負の整数である必要があります
<(primes)N must be a scalar
>(primes)Nはスカラである必要があります
<(rgb2hsv)R,G,B must all be the same size.
>(rgb2hsv)R,G,Bは同じサイズである必要があります

% $Revision: 1.6 $ $Date: 2002/03/23 02:39:44 $
