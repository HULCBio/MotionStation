<()Wrong number of arguments
>()引数の数が正しくありません
<(brighten)Beta must be between -1 and 1.
>(brighten)Betaは-１から1の範囲である必要があります
<(camlight)First numeric argument must be a handle to a single light object
>(camlight)最初の数値引数はあるLightオブジェクトのハンドルである必要があります
<(camlight)Invalid arguments
>(camlight)引数が正しくありません
<(camlight)Too many arguments
>(camlight)引数が多すぎます
<(camlookat)H must be a vector of axes children or an axes
>(camlookat)HはAxes childrenのベクトル、またはAxesである必要があります
<(camzoom)zf must be greater than 0.
>(camzoom)zfは0より大きい必要があります
<(caxis)Must be two element row vector.
>(caxis)2要素の行ベクトルが必要です
<(caxis)Unknown command option
>(caxis)コマンドオプションが正しくありません
<(colorbar)COLORBAR expects a handle, 'vert', or 'horiz' as input.
>(colorbar)COLORBARはハンドル、"vert"、または"horiz"を入力引数とします
<(colorbar)Requires axes handle.
>(colorbar)Axesハンドルが必要です
<(colordef)The figure must be cleared to use COLORDEF(FIG,OPTION).
>(colordef)FigureはCOLORDEF(FIG,OPTION)を使って閉じる必要があります
<(colordef)Unknown color default option: %s.
>(colordef)デフォルトのカラーオプションが正しくありません: %s.
<(colormap)Colormap must have 3 columns: [R,G,B].
>(colormap)カラーマップは3行である必要があります。 [R,G,B]
<(colormap)Colormap must have values in [0,1].
>(colormap)カラーマップは[0,1]の範囲の値をもつ必要があります
<(diffuse)Ny must be the same size as Nx.
>(diffuse)NyはNxと同じサイズである必要があります
<(diffuse)Nz must be the same size as Nx.
>(diffuse)NzはNxと同じサイズである必要があります
<(lightangle)AZ and EL must both be scalars
>(lightangle)AZとELはスカラである必要があります
<(lightangle)H must be a handle to a single light object
>(lightangle)HはあるLightオブジェクトのハンドルである必要があります
<(lightangle)Wrong number of output arguments
>(lightangle)出力引数の数が正しくありません
<(lighting)Lighting methods are none, flat, gouraud, and phong.
>(lighting)ライティングの手法はnone、flat、gouraud、またはphongです
<(material)The material vector must be of size 1x3, 1x4, or 1x5
>(material)materialベクトルは1x3、1x4、または1x5である必要があります
<(mesh)Invalid argument.
>(mesh)引数が正しくありません
<(mesh)Invalid input arguments.
>(mesh)入力引数が正しくありません
<(mesh)Property value pairs expected.
>(mesh)プロパティ値のペアを入力してください
<(rotate3d)Unknown action string.
>(rotate3d)action文字列が正しくありません
<(shading)Shading methods are flat, faceted, and interp.
>(shading)Shading手法はflat、faceted、またはinterpです
<(specular)S must be specified using [AZ,EL] or [Sx,Sy,Sz].
>(specular)Sは[AZ,EL]、または[Sx,Sy,Sz]で指定される必要があります
<(specular)V must be specified using [AZ,EL] or [Vx,Vy,Vz].
>(specular)Vは[AZ,EL]、または[Vx,Vy,Vz]で指定される必要があります
<(surfl)Must be 1, 3 or 4 numeric input arguments.
>(surfl)1、3、または4つの数値入力引数が必要です
<(surfl)Unknown lighting method "%s".
>(surfl)lighting手法"%s"が正しくありません
<(surfl)Weighting vector k must have four components.
>(surfl)重みベクトルkは4つの要素を持つ必要があります
<(surfl)X, Y, and Z must be at least 3-by-3.
>(surfl)X、Y、及びZは少なくとも3 x 3である必要があります
<(surfnorm)Wrong number of matrix input arguments.
>(surfnorm)入力引数の数が正しくありません
<(surfnorm)Y and Z must be the same size as X.
>(surfnorm)YとZは、Xと同じサイズである必要があります
<(view)Argument must be scalar, two-vector, or 4-by-4 matrix
>(view)引数はスカラ、2要素ベクトル、または4 x 4行列である必要があります
<(view)Arguments must be scalars
>(view)引数はスカラである必要があります
<(view)Single scalar argument must be 2 or 3
>(view)引数は2、または3個である必要があります
<(viewmtx)XC must be a 3-vector of [x,y,z] values.
>(viewmtx)XCは[x,y,z]の値を持った3要素ベクトルである必要があります
<(vrml)Filename argument is not a string
>(vrml)ファイル名の引数が文字列でありません
<(vrml)First argument must be a valid object handle
>(vrml)最初の引数は適切なオブジェクトハンドルである必要があります
<(vrml)Interpolated colors in patch object not supported yet by vrml.m
>(vrml)vrml.mはパッチオブジェクトの色補間をサポートしていません
<(vrml)Not enough input arguments
>(vrml)入力引数が十分でありません
<(vrml)Unknown color type in patchColorIndex
>(vrml)patchColorIndexのカラータイプが正しくありません
<(vrml)Unknown colorIndex type in surfColorIndex
>(vrml)surfColorIndexのcolorIndexタイプが正しくありません
<(vrml)Unknown light mode in HandleLight
>(vrml)HandleLightのlightモードが正しくありません
<(vrml)Unknown mapping type in function colorMapping
>(vrml)関数colorMappingのマッピングタイプが正しくありません
<(vrml)not dealing with rgb data yet
>(vrml)rgbデータは扱えません
<(vrml)patch color scheme not handled yet
>(vrml)パッチカラースキームは扱えません
<(vrml)rgb textures not supported yet
>(vrml)rgb texturesはサポートされていません
<(vrml)unknown edgecolor type in Handle Surface
>(vrml)Surfaceハンドルのedgecolorタイプが正しくありません
<(vrml)unknown facecolor type in Handle Patch
>(vrml)Patchハンドルのfacecolorタイプが正しくありません
<(whitebg)Unknown color string.
>(whitebg)カラー文字列が正しくありません
<(zlabel)Incorrect number of input arguments
>(zlabel)入力引数の数が正しくありません
<(cameratoolbar)Aspect Ratio Adjustment
>(cameratoolbar)ｱｽﾍﾟｸﾄ比の調整
<(cameratoolbar)Plots may change appearance so that aspect ratios remain
>(cameratoolbar)3D回転時にﾌﾟﾛｯﾄのｱｽﾍﾟｸﾄ比が変化するかもしれません。
<(cameratoolbar)unchanged during 3D rotation.  Check the "Auto aspect
>(cameratoolbar)Figureにﾌｨｯﾄした座標形状に変更するには座標軸(Axes)の
<(cameratoolbar)ratio" context menu item to have the axes reshape to fit
>(cameratoolbar)ｺﾝﾃｸｽﾄﾒﾆｭｰの"自動ｱｽﾍﾟｸﾄ比"をﾁｪｯｸしてください。
<(cameratoolbar)the figure.
>(cameratoolbar)


% $Revision: 1.9 $ $Date: 2002/03/23 02:30:31 $
