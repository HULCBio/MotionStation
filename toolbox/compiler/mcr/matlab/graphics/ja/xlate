% $Revision: 1.8 $ $Date: 2002/03/23 02:35:34 $ 

<(cla)Unknown command option.
>(cla)コマンドオプションが正しくありません
<(close)Error while evaluating figure CloseRequestFcn
>(close)FigureのCloseRequestFcnの実行中にエラー
<(close)Invalid figure handle.
>(close)Figureのハンドルが正しくありません
<(close)Specified window does not exist.
>(close)指定されたウィンドウが存在しません
<(hold)Current plot held
>(hold)現在のプロットをホールドされました
<(hold)Current plot released
>(hold)現在のプロットをリリースしました
<(noanimate)NOANIMATE needs to know if it should 'save' or 'restore'
>(noanimate)NOANIMATEは"save"、または"restore"を指定する必要があります
<(nodither)NODITHER needs to know if it should 'save' or 'restore'
>(nodither)NODITHERは"save"、または"restore"を指定する必要があります
<(print)Debugging in PRINT, PrintJob object is:
>(print)PRINTのデバッグ、PrintJobオブジェクトは:
<(print)Supported devices are:
>(print)サポートドライバ:
<(refresh)Handle does not refer to a figure object
>(refresh)ハンドルはFigureオブジェクトを参照してません
<(savtoner)SAVTONER needs to know if it should 'save' or 'restore'
>(savtoner)SAVTONERは"save"、または"restore"を指定する必要があります

<(openfig)Property names and values must come in pairs
>(openfig)ﾌﾟﾛﾊﾟﾃｨ名とﾌﾟﾛﾊﾟﾃｨ値はペアとして使う必要があります
<(openfig)Argument must be a .fig file
>(openfig)引数は .fig ファイルである必要があります
<(openfig)Unable to find figure file: %s
>(openfig)figureファイルが見つかりません: %s
<(openfig)Second input argument must be 'new' or 'reuse'.
>(openfig)二番目の入力引数は'new'、または'reuse'である必要があります
