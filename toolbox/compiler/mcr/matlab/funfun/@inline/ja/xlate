<(cat,horzcat,varcat)Inline functions can't be concatenated.
>(cat,horzcat,varcat)インライン関数は連結できません
<(feval,subsref)Not enough inputs to inline function.
>(feval,subsref)インライン関数への入力が足りません
<(feval,subsref)Too many inputs to inline function.
>(feval,subsref)インライン関数への入力が多すぎます
<(subsref)Can't call an empty inline function.
>(subsref)空のインライン関数は呼べません

% $Revision: $ $Date: $
