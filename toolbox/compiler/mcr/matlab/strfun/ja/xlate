<(bin2dec)Binary string must be 52 bits or less.
>(bin2dec)バイナリ文字列は52bit以下である必要があります
<(bin2dec)Input must be a string.
>(bin2dec)入力は文字列である必要があります
<(cellstr)S must be 2-D.
>(cellstr)Sは2-Dである必要があります
<(dec2base)B must be an integer, 2 <= B <= 36.
>(dec2base)Bは整数である必要があります、または2 <= B <= 36
<(dec2bin)N must be a positive scalar.
>(dec2bin)Nは正のスカラである必要があります
<(dec2hex)D must be a non-negative integer.
>(dec2hex)Dは非負の整数である必要があります
<(hex2num)Input string to hex2num should have just 0-9, a-f, or A-F.
>(hex2num)hex2numへの入力文字列は0-9,a-f,A-Fである必要があります
<(hex2num)Input to hex2num must be a string.
>(hex2num)hex2numへの入力は文字列である必要があります
<(num2str)'%s' is an invalid format.
>(num2str)"%s" は正しくないフォーマットです
<(num2str)Format must contain field width.
>(num2str)フォーマットはフィールド範囲を含む必要があります
<(str2num)Requires string or character array input.
>(str2num)文字列、または文字配列入力が必要です
<(strcat)All the inputs must be two dimensional.
>(strcat)すべての入力引数は2次元である必要があります
<(strcat)All the inputs must have the same number of rows or a single row.
>(strcat)すべての入力引数は同じ行数、または１行である必要があります


% $Revision: 1.6 $ $Date: 2002/03/23 02:39:54 $
