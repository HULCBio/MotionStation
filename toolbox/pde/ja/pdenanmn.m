% NANMIN は、NaN を含んだデータから、最小値を算出します。
% NANMIN(...) は、NaN を使用しない MIN と同じ結果を出力します。
% 
% ベクトルに対して、 MIN(X) は、X の中の NaN でない最小の要素を出力しま
% す。行列に対して、各列から NaN でない最小の値を要素とするベクトルを出
% 力します。[M,I] = MIN(...) は、ベクトル I の中の最小値のインデックスを
% 出力します。

%       Copyright 1994-2001 The MathWorks, Inc.
