% PDEPATCH は、最小領域またはサブドメインのプロットを行います。
% PDEPATCH(DL,BT,MSB,FLAG) は、FLAG = 0(デフォルト)で、最小領域を作成す
% るように交差しているオリジナルの形状オブジェクトの数に対応してグレーで
% のシェーディングを施した最小領域をプロットします。
% 
% オリジナルの形状の分割は、DECSG: [DL,BT,DL1,BT1,MSB] = DECSG(...) をコ
% ールすることにより処理されます。FLAG = 1 の場合、サブドメインに分割し
% て表示されます。
%
% 参考   DECSG

%       Copyright 1994-2001 The MathWorks, Inc.
