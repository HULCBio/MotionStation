% CIRCLEG    circleg PDEモデル用の形状データを与えます。
%
% NE = CIRCLEG は、境界セグメントの数を与えます。
%
% D = CIRCLEG(BS) は、BS 内の設定されている各境界セグメントに1列を割り当
% てている行列を与えます。
% 1行目は、最初の境界セグメントのパラメータ値を含みます。
% 2行目は、最後の境界セグメントのパラメータ値を含みます。
% 3行目は、左領域の数を含みます。
% 4行目は、右領域の数を含みます。
%
% [X,Y] = CIRCLEG(BS,S) は、境界点の座標を与えます。BS は、境界セグメン
% トを指定し、S は対応するパラメータ値を指定します。BS はスカラでも構い
% ません。

% Copyright 1994-2001 The MathWorks, Inc.
