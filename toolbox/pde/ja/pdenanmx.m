% NANMAX は、NaN を含んだデータから、最大値を算出します。
% NANMAX(...) は、NaN を使用しない MAX と同じ結果を出力します。
% 
% ベクトルに対して、 MAX(X) は、X の中の NaN でない最大の要素を出力しま
% す。行列に対して、各列から NaN でない最大の値を要素とするベクトルを出
% 力します。[M,I] = MAX(...) は、ベクトル I の中の最大値のインデックスを
% 出力します。

%       Copyright 1994-2001 The MathWorks, Inc.
