<(griddata)METHOD must be one of 'linear','cubic','nearest', or 'v4'.
>(griddata)METHODは"linear"、"cubic"、"nearest"、または"v4"のどれかである必要があります
<(griddata)Unknown method.
>(griddata)メソッドが正しく正しくありません
<(interp1)The length of X must match the number of rows of Y.
>(interp1)Xの長さはYの行数と同じである必要があります
<(interp1)X must be a vector.
>(interp1)Xはベクトルである必要があります
<(interp2)The lengths of the X and Y vectors must match Z.
>(interp2)XとYベクトルの長さは、Zと一致する必要があります
<(interp2)X and Y must be monotonic vectors or matrices produced by MESHGRID.
>(interp2)XとYはMESHGRIDで作成された単調増加のベクトル、または行列である必要があります
<(interp2)XI and YI must be the same size.
>(interp2)XIとYIは同じサイズである必要があります
<(interp2)Z must be at least 2-by-2.
>(interp2)Zは少なくとも2x2である必要があります
<(interp2)Z must be at least 3-by-3.
>(interp2)Zは少なくとも3x3である必要があります
<(interp3)The lengths of the X,Y,Z vectors must match V.
>(interp3)X、Y、Zベクトルの長さは、Vと一致する必要があります
<(interp3)V must be a 3-D array.
>(interp3)Vは3D配列である必要があります
<(interp3)V must be at least 2-by-2-by-2.
>(interp3)Vは少なくとも2x2x2である必要があります
<(interp3)V must be at least 3-by-3-by-3.
>(interp3)Vは少なくとも3x3x3である必要があります
<(interp3)X, Y, and Z must be monotonic vectors or arrays produced by MESHGRID.
>(interp3)X、Y、及びZはMESHGRIDで作成された単調増加のベクトル、または配列である必要があります
<(interp3)XI, YI and ZI must be the same size.
>(interp3)XI、YI、及びZIは同じサイズである必要があります
<(interpft)N must be a scalar.
>(interpft)Nはスカラである必要があります
<(interpn)The lengths of the X1,X2,X3,... vectors must match V.
>(interpn)X1、X2、X3、....ベクトルの長さは、Vと一致する必要があります
<(interpn)V must be a %d-D array.
>(interpn)Vは%d-D配列である必要があります
<(interpn)V must be at least 2-by-2-by-2-by-...
>(interpn)Vは少なくとも2x2x2x...である必要があります
<(interpn)V must be at least 3-by-3-by-3-by-...
>(interpn)Vは少なくとも3x3x3x...である必要があります
<(interpn)X1,X2,X3, etc. must be monotonic vectors or arrays produced by NDGRID.
>(interpn)X1、X2、X3...はNDGRIDで作成された単調増加のベクトル、または配列である必要があります
<(interpn)Y1,Y2,Y3, etc. must be the same size.
>(interpn)Y1、Y2、Y3...はすべて同じサイズである必要があります
<(poly)Argument must be a vector or a square matrix.
>(poly)引数はベクトル、または正方行列である必要があります
<(polyarea)Not enough inputs.
>(polyarea)入力が十分でありません
<(polyarea)X and Y must be the same size.
>(polyarea)XとYは同じサイズである必要があります
<(polyfit)X and Y vectors must be the same size.
>(polyfit)ベクトルXとYは、同じサイズである必要があります
<(polyval)S matrix must be n+2-by-n where n = length(p)
>(polyval)S行列はn+2行n列である必要があります。ここでn = length(p)です
<(polyvalm)Matrix must be square.
>(polyvalm)行列は正方である必要があります
<(roots)Must be a vector.
>(roots)ベクトルが必要です
<(spline)Abscissa and ordinate vector should be of the same length.
>(spline)AbscissaとOrdinateベクトルは同じ長さである必要があります
<(spline)The data abscissae should be distinct.
>(spline)abscissaeデータは独立な値である必要があります
<(spline)There should be at least two data points.
>(spline)少なくとも、2つのデータ点が必要です
<(ss2tf)IU must be specified for systems with more than one input.
>(ss2tf)IUはひとつ以上の入力をもったシステムを指定する必要があります
<(table1)First column of the table must be monotonic.
>(table1)テーブルの最初の列は単調増加である必要があります
<(table1)TAB must have at least 2 rows.
>(table1)TABは少なくとも、2行必要です
<(table1)x0 larger than all values in first column
>(table1)x0は最初の列のすべての値よりも大きいです
<(table1)x0 smaller than all values in first column
>(table1)x0は最初の列のすべての値よりも小さいです
<(table2)Must be used with three input arguments.
>(table2)3つの入力引数が必要です
<(tf2ss)Denominator must be a row vector.
>(tf2ss)分母は１行のベクトルである必要があります
<(tf2ss)Denominator must be higher or equal order than numerator.
>(tf2ss)分母は分子と等しいか、それ以上の次数である必要があります
<(tf2zp)Denominator must have non-zero leading coefficient.
>(tf2zp)分母の最初の係数はゼロ以外である必要があります
<(unmkpp)The input array does not seem to describe a pp function.
>(unmkpp)入力配列はpp関数形式でありません
<(unmkpp)The input structure does not seem to describe a pp function.
>(unmkpp)入力の構造はpp関数形式でありません
<(zp2tf)K must have as many elements as Z has columns.
>(zp2tf)KはZの列数と同じ数の要素をもつ必要があります
<(zp2tf)Z and P must be column vectors.
>(zp2tf)ZとPは列ベクトルである必要があります

% $Revision: 1.10 $ $Date: 2002/03/23 02:31:16 $
