% SIZE   timer オブジェクト配列のサイズ
%
% D = SIZE(OBJ) は、M行N列の timer オブジェクト配列に対して、timer オブ
% ジェクト配列 OBJ の中の行数と列数を含む2要素の行ベクトル D = [M, N] 
% を出力します。
%
% [M,N] = SIZE(OBJ) は、別々の出力変数に行数と列数を出力します。
%
% [M1,M2,M3,...,MN] = SIZE(OBJ) は、OBJ の最初の N 次元の長さを
% 出力します。
%
% M = SIZE(OBJ,DIM) は、スカラー DIM で指定される次元の長さを出力します。
% たとえば、SIZE(OBJ,1) は、行数を出力します。
% 
% 参考 : TIMER/LENGTH.


%    RDD 1/8/2002
%    Copyright 2001-2002 The MathWorks, Inc. 
%    $Revision: 1.1.4.1 $  $Date: 2004/04/28 01:57:45 $
