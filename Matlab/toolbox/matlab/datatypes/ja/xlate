<(celldisp)Must be a cell array.
>(celldisp)セル配列である必要があります
<(cellplot)C must be a cell array.
>(cellplot)Cはセル配列である必要があります
<(deal)The number of outputs should match the number of inputs.
>(deal)出力数は入力数と同じある必要があります
<(getfield)Inputs must be either cell arrays or strings.
>(getfield)入力はセル配列、または文字列のどちらかである必要があります
<(rmfield)A field named '%s' doesn't exist.
>(rmfield)フィールド名"%s"が存在しません
<(rmfield)FIELDNAMES must be a cell array of strings.
>(rmfield)FILEDNAMESは文字列のセル配列である必要があります
<(rmfield)S must be a structure array.
>(rmfield)Sは構造体配列である必要があります
<(substruct)SUBSTRUCT takes an even number of arguments.
>(substruct)SUNSTRUCTは偶数の引数です
<(substruct)SUBSTRUCT takes at least two arguments.
>(substruct)SUBSTRUCTは少なくとも2つの引数が必要です

% $Revision: 1.5 $ $Date: 2002/02/05 13:50:27 $
