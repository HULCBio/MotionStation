% INTERSECT   文字列のセル配列に対する集合の共通部分
%
% INTERSECT(A,B) は、A と B が文字列のセル配列のとき、A と B の両方に
% 共通な値を出力します。結果はソートされます。
%
% [C,IA,IB] = INTERSECT(A,B) は、C = A(IA) と C = B(IB) となるような
% インデックスベクトル IA と IB も出力します。
%
% 参考 : UNIQUE, UNION, SETDIFF, SETXOR, ISMEMBER.


%   Copyright 1984-2002 The MathWorks, Inc. 
