<(intersect)A and B must be vectors or 'rows' must be specified.
>(intersect)AとBはベクトル、または"rows"を指定する必要があります
<(intersect)A and B must have the same number of columns.
>(intersect)AとBは同じ列数である必要があります
<(ismember)A and S must have the same number of columns.
>(ismember)AとSは同じ列数である必要があります
<(ismember)Unknown flag.
>(ismember)フラグが正しくありません

% $Revision: 1.4 $ $Date: 2002/02/05 13:42:30 $
