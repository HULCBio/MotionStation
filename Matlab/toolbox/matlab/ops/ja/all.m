% ALL   ベクトルのすべての要素が非ゼロであるかどうかのテスト
%
% ベクトルに対して、すべての要素が非ゼロの場合、ALL(V)は1を出力します。
% そうでなければ、0を出力します。行列に対して、ALL(X)はXの列に対して
% 演算を行い、1と0を要素とする行ベクトルを出力します。N次元配列に対して、
% ALL(X)は最初に1でない次元に対して演算を行います。
%
% ALL(X,DIM)は、次元DIMに対して演算を行います。たとえば、ALL(X,1)は、
% Xの最初の次元(行)に対して行います。
%
% 参考：ANY.


%   Copyright 1984-2002 The MathWorks, Inc. 
%   $Revision: 1.9.4.1 $  $Date: 2004/04/28 02:00:23 $
%   Built-in function.
