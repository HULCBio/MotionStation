% RANK   行列のランク
% 
% RANK(A) は、行列 A の線形独立な行数または列数を求めます。
% RANK(A,tol) は、tol よりも大きいAの特異値の数を出力します。
% RANK(A) は、デフォルトの tol = max(size(A)) * norm(A) * eps を使います。
% 
% 入力 A のサポートクラス
%   float: double, single
%
%   Copyright 1984-2004 The MathWorks, Inc. 

