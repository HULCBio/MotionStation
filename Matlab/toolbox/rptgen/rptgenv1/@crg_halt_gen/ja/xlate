%   $Revision: 1.1.6.1 $  $Date: 2004/03/21 22:31:30 $
<(execute)Component - Stop Report Generation
>(execute)ｺﾝﾎﾟｰﾈﾝﾄ - Stop Report Generation
<(execute)Generation halted by component
>(execute)ｺﾝﾎﾟｰﾈﾝﾄによる生成の中止
<(getinfo)Stop Report Generation
>(getinfo)ﾚﾎﾟｰﾄ生成の中止
<(getinfo)Stops the generation process - same as hitting the "Stop" button
>(getinfo)生成ﾌﾟﾛｾｽの中止 - "ｽﾄｯﾌﾟ" ﾎﾞﾀﾝと同じです
<(getinfo)Halt Generation
>(getinfo)生成の中止
<(getinfo)Continue Generation
>(getinfo)生成の継続
<(getinfo)Confirm before stopping generation
>(getinfo)中断する前に確認する
<(getinfo)Confirmation question  
>(getinfo)確認の質問  
<(getinfo)Halt button
>(getinfo)中止ﾎﾞﾀﾝ
<(getinfo)Continue button
>(getinfo)継続ﾎﾞﾀﾝ
<(getinfo)Stop generating the report?
>(getinfo)ﾚﾎﾟｰﾄ生成を中止しますか ?
<(outlinestring)Stop Generation%s
>(outlinestring)%s生成中止
<(crg_halt_gen)    This component stops the report from generating.  It acts
>(crg_halt_gen)    このｺﾝﾎﾟｰﾈﾝﾄは生成中にﾚﾎﾟｰﾄを中止できます。
<(crg_halt_gen)    exactly the same as pressing the "stop" button during
>(crg_halt_gen)    生成中に"ｽﾄｯﾌﾟ"ﾎﾞﾀﾝを押した場合と同じ動作をします。
<(crg_halt_gen)    generation.  This component is best used inside of an
>(crg_halt_gen)    このｺﾝﾎﾟｰﾈﾝﾄは"if/then"ｽﾃｰﾄﾒﾝﾄの中で利用すると
<(crg_halt_gen)    "if/then" statement.
>(crg_halt_gen)    便利です。
<(crg_halt_gen)    Note that when generation is halted, the SGML source file
>(crg_halt_gen)    生成が中止された場合、SGMLｿｰｽﾌｧｲﾙは生成されますが、
<(crg_halt_gen)    is produced, but is not converted.
>(crg_halt_gen)    変換はされないことに注意してください
<(getinfo)Help
>(getinfo)ﾍﾙﾌﾟ
