% DISTFCM は、ファジィ c-mean クラスタリング内での距離の尺度
% 
% OUT = DISTFCM(CENTER, DATA) は、CENTER 内の各行と DATA 内の各行との間
% のユークリッド距離を計算し、M 行 N 列の大きさの距離行列 OUT を出力しま
% す。ここで、M とN は、CENTER と DATA の行数で、OUT(I,J) は、CENTER(I,:)
% と DATA(J,:) 間の距離です。
% 
% 参考   FCMDEMO, INITFCM, IRISFCM, STEPFCM, and FCM.

