% SUBSREF   GF配列に対するサブスクリプトの参照
% 
% A(I) は、サブスクリプトベクトル I で指定された A の要素からなる配列
% です。結果の配列は、A と I が両方ともベクトルである特別な場合を除いて
% I と同じ大きさです。この場合、A(I) は I と同じ要素数ですが、A と同じ
% 方向になります。
%
% A(I,J) は、サブスクリプトベクトル I と J で指定された A の長方形のサブ
% 行列の要素からなる配列です。結果の配列は、LENGTH(I)行LENGTH(J)列に
% なります。A(I,:) のようにサブスクリプトとして使用されるコロンは、
% 列(または行)全体を示します。


%    Copyright 1996-2002 The MathWorks, Inc.
