% ALL   GFベクトルのすべての要素の非ゼロを検出
%
% GFベクトルに対して、ベクトルの要素にゼロがない場合、ALL(V) は1を
% 出力します。そうでなければ、0を出力します。行列に対して、ALL(X) は、
% X の列に対して演算を行い、1と0を要素とする行ベクトルを出力します。
%
% ALL(X,DIM) は、次元 DIM に対して演算を行います。たとえば、ALL(X,1) は、
% X の最初の次元(行)に対して行います。
%
% 参考 : ANY.


%    Copyright 1996-2002 The MathWorks, Inc.
