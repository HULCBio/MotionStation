% SUM は、要素の和を計算します。
% ベクトルに対して、SUM(X) は、X の要素の和を計算します。
% 行列に対して、SUM(X) は、列単位の和を計算し、行ベクトルを出力します。
% SUM(DIAG(X)) は、X の対角要素の和を計算します。
%
% 参考： PROD, CUMPROD, CUMSUM.

%   Copyright 1986-2001 The MathWorks, Inc.
