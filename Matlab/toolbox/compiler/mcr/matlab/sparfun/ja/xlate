<(bicg)Matrix must be square.
>(bicg)行列は正方である必要があります
<(bicg)Right hand side must be a column vector.
>(bicg)右側は列ベクトルである必要があります
<(eigs)LU factorization of (A-sigma*I) for sigma = %10e: condest(U) = %10e\n
>(eigs)sigma = %10eを用いた(A-sigma*I)のLU分解: condest(U) = %10e\n
<(rjr)A must be square.
>(rjr)Aは正方である必要があります
<(spconvert)Array D must have 3 or 4 columns.
>(spconvert)配列Dは3、または4列である必要があります
<(spdiags)A cannot be a vector.
>(spdiags)Aがベクトルではありません
<(speye)Please use SPEYE(N), SPEYE(M,N) or SPEYE([M,N]).
>(speye)SPEYE(N)、SPEYE(M,N)、またはSPEYE([M,N])を使ってください
<(sprand)Reciprocal condition number must be less than 1.
>(sprand)条件数の逆数は1以下である必要があります
<(sprand)Too many or not enough input arguments.
>(sprand)入力引数が多すぎる、または十分ではありせん
<(sprandn)Reciprocal condition numver must be less than 1.
>(sprandn)条件数の逆数は1以下である必要があります
<(sprandsym)First argument must be a scalar
>(sprandsym)最初の引数はスカラである必要があります
<(sprandsym)rc must be > 0 and <= 1.
>(sprandsym)rcは > 0で <=1である必要があります
<(svds)Fourth argument must be a structure of options.
>(svds)４つの引数はオプション構造である必要があります
<(svds)Third argument must be a scalar or the string 'L'
>(svds)3つ目の引数はスカラ、または文字列"L"である必要があります

% $Revision: $ $Date: $
