<(conv)A and B must be vectors.
>(conv)AとBはベクトルである必要があります
<(cov)Inputs must be 2-D.
>(cov)入力は2-Dである必要があります
<(cov)The lengths of x and y must match.
>(cov)xとyベクトルの長さは同じである必要があります
<(cumtrapz)length(x) must equal length of first non-singleton dim of y.
>(cumtrapz)length(x)はｙの最初のノン-シングルトン次元数と同じである必要があります
<(deconv)First coefficient of A must be non-zero.
>(deconv)Ａの最初の係数はゼロ以外である必要があります
<(filter2)Unknown shape parameter.
>(filter2)shapeパラメータが正しくありません
<(hist)Input arguments must be numeric.
>(hist)入力引数は数値である必要があります
<(hist)Requires one or two input arguments.
>(hist)1、または2つの入力引数が必要です
<(sortrows)X must be a 2-D matrix.
>(sortrows)Xは2-D行列である必要があります
<(var)The second argument must be a vector.
>(var)2番目の引数はベクトルである必要があります
<(var)The second argument must be positive.
>(var)2番目の引数は正の値である必要があります
<(var)The sizes of the arguments are not compatible.
>(var)引数のサイズが正しくありません


% $Revision: 1.8 $ $Date: 2002/03/23 02:39:13 $
